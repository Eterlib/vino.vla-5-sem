class B(metaclass=dump):
    def __init__(self, base=1.5):
        self.base = base

    def mul(self, k, neg=False):
        val = self.base * k
        return -val if neg else val


b = B()
print(b.mul(2))
print(b.mul(3, neg=True))